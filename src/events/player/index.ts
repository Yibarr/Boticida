import * as playerStart from './playerStart'
import * as playerFinish from './playerFinish'
import * as playerError from './playerError'

export default {
    playerStart,
    playerFinish,
    playerError
}